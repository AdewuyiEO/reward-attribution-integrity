# core
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
duckdb>=0.10
pyarrow>=14.0

# visualisation / dashboard
matplotlib>=3.7
streamlit>=1.30

# orchestration (optional - only needed to run the Airflow DAG)
# apache-airflow>=2.8

# testing
pytest>=7.4
